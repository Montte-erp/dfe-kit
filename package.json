{
  "name": "dfekit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "core/*",
    "adapters/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "typecheck": "nx run-many -t typecheck",
    "publint": "nx run-many -t publint",
    "test": "nx run-many -t test",
    "lint": "oxlint -c tooling/oxc/base.json core adapters packages tooling --deny-warnings",
    "format": "oxfmt -c tooling/oxc/oxfmt.json --write core adapters packages tooling '!**/.cache/**' '!**/dist/**' '!**/*.d.ts' '!**/*.tsbuildinfo'",
    "format:check": "oxfmt -c tooling/oxc/oxfmt.json --check core adapters packages tooling '!**/.cache/**' '!**/dist/**' '!**/*.d.ts' '!**/*.tsbuildinfo'",
    "check:declarative-errors": "bun tooling/static-checks/check-declarative-errors.ts",
    "check:static": "bun run check:declarative-errors && bun run lint && bun run format:check",
    "check": "bun run check:static && nx run-many -t typecheck test build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bunup": "^0.16.32",
    "nx": "^22.7.5",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "publint": "^0.3.21",
    "typescript": "latest"
  },
  "license": "BUSL-1.1"
}
